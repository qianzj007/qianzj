apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ibp-yushenkanban.fullname" . }}
  labels:
    {{- include "ibp-yushenkanban.labels" . | nindent 4 }}
spec:
  {{- if .Values.strategy.enabled }}
  strategy:
    type: {{ .Values.strategy.type }}
    rollingUpdate:
       {{- toYaml .Values.strategy.rollingUpdate | nindent 6 }}
  {{- end }}
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ibp-yushenkanban.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "ibp-yushenkanban.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.ImagePullSecrets.Enabled }}
      imagePullSecrets:
        {{- toYaml .Values.ImagePullSecrets.Attributes | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "ibp-yushenkanban.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          env:
          {{- with .Values.JavaAttributes }}
            {{- toYaml . | nindent 13 }}
          {{- end }}
          {{- with .Values.GlobalAttributes }}
            {{- toYaml . | nindent 13 }}
          {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.lifecycle.Enabled }}
            {{- toYaml .Values.lifecycle.lifecycleAttributes | nindent 10 }}
          {{- end }}
          ports:
          {{- range .Values.service.PortScope }}
            - name: {{ .portName }}
              containerPort: {{ .port }}
              protocol: {{ .protocol }}
          {{- end }}
          {{- if .Values.HealthCheck.Enabled}}
            {{- toYaml .Values.HealthCheck.EetectAttributes | nindent 10 }}
          {{- end }}
          {{- if .Values.resources.Enabled}}
          resources:
            {{- toYaml .Values.resources.ResourcesAttributes | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: time-config
              mountPath: /etc/localtime
              readOnly: true
          {{- if .Values.ResourceVolumes.Enabled }}
          {{- toYaml .Values.ResourceVolumes.ResourceVolumesMountPathAttributes | nindent 12 }}
          {{- end }}
          {{- if .Values.Privatization.Enabled }}
          {{- toYaml .Values.Privatization.LicenseAttributesvolumeMounts | nindent 12 }}
          {{- end }}
      volumes:
        - name: time-config
          hostPath:
            path: /etc/localtime
          {{- if .Values.ResourceVolumes.Enabled }}
          {{- toYaml .Values.ResourceVolumes.ResourceVolumesAttributes | nindent 8 }}
          {{- end }}
        {{- if .Values.Privatization.Enabled }}
        {{- toYaml .Values.Privatization.LicenseAttributesVolumes | nindent 8 }}
        {{- end }}
      {{- if .Values.NodeSelector.Enabled }}
      nodeSelector:
        {{- toYaml .Values.NodeSelector.Attributes | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.Tolerations.Enabled }}
      tolerations:
        {{- toYaml .Values.Tolerations.TolerationsAttributes | nindent 8 }}
      {{- end }}
      {{- if .Values.topologySpreadConstraints.Enabled }}
      topologySpreadConstraints:
      - labelSelector:
          matchLabels:
          {{- include "ibp-yushenkanban.selectorLabels" . | nindent 12 }}
        {{- toYaml .Values.topologySpreadConstraints.Attributes | nindent 8 }}
      {{- end }}

