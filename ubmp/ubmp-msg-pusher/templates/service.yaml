apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubmp-msg-pusher.fullname" . }}
  labels:
    {{- include "ubmp-msg-pusher.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    {{- range .Values.service.PortScope }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .portName }}
     {{- end }}
  selector:
    {{- include "ubmp-msg-pusher.selectorLabels" . | nindent 4 }}
---
{{- if .Values.service.Enabled }}
apiVersion: v1
kind: Service
metadata:
  name: headless-{{ include "ubmp-msg-pusher.fullname" . }}
  labels:
    {{- include "ubmp-msg-pusher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:  {{- $type := .Values.service.type -}}
    {{- range .Values.service.PortScope }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .portName }}
      {{- if eq $type "NodePort" }}
      nodePort: {{ .nodePort }}
      {{- end }}
     {{- end }}
  selector:
    {{- include "ubmp-msg-pusher.selectorLabels" . | nindent 4 }}
{{- end }}
---
{{- if .Values.serviceCompatibles.Enabled }}
apiVersion: v1
kind: Service
metadata:
  name: msg-pusher
  labels:
    {{- include "ubmp-msg-pusher.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    {{- range .Values.service.PortScope }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .portName }}
     {{- end }}
  selector:
    {{- include "ubmp-msg-pusher.selectorLabels" . | nindent 4 }}
{{- end }}