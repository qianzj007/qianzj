# Default values for ubmp-license.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: harbor.space.com/ubmp/ubmp-license
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""
### 配置服务端口
service:
  Enabled: false
  type: NodePort
  IngressPort: 8079
  PortScope:
    - portName: registry
      port: 8079
      targetPort: registry
      protocol: TCP
      nodePort: 300104

## 证书
ImagePullSecrets:
  Enabled: false
  Attributes:
    - name: regcred

serviceCompatibles:
  Enabled: false
#################################################### 环境变量区域
JavaAttributes:
  - name: JAVA_HOME
    value: "java -Xms512m -Xmx512m -Dfile.encoding=UTF-8 -Duser.timezone=GMT+08 -jar ubmp-license-server.jar"
GlobalAttributes:
  - name: nacos_server_addr
    value: nacos-cs.middleware.svc.dev.local:8848
  - name: nacos_namespace
    value: upms-dev
  - name: MysqlUrl
    value: mysql-dev.space.com
  - name: MysqlPort
    value: "3306"
  - name: MysqlUsername
    value: upms
FrontAddress:
  - name: portalUrl
    value: portal-dev.space.com
BackendApiUrl:
  - name: portalAPIUrl
    value: portal-gateway-dev.space.com
# 生产、sim、uat环境接口地址
ExternalFrontAddress:
  - name: EiotUrl
    value: iot-dev.spaceplat.com
ExternalBackendApiUrl:
  - name: EiotAPIUrl
    value: iot-gateway-dev.spaceplat.com
######################################################################


ingress:
  enabled: false
  className: ""
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local


lifecycle:
  Enabled: false
  lifecycleAttributes:
    lifecycle:
      preStop:
        exec:
          command: ["/bin/sh","-c","jmap -histo:live 1 | head -40 >/dara/logs/jmap_histo_$(ps -fe |head -n 3|awk -F ' ' '{print $NF}' |grep jar )_$(date +%F_%H-%M-%S).txt && jmap -dump:live,format=b,file=/dara/logs/jmap_dump_$(ps -fe |head -n 3|awk -F ' ' '{print $NF}' |grep jar )_$(date +%F_%H-%M-%S).txt 1 && jmap -heap 1 >/dara/logs/jmap_heap_$(ps -fe |head -n 3|awk -F ' ' '{print $NF}' |grep jar )_$(date +%F_%H-%M-%S).txt"]

resources:
  Enabled: false
  ResourcesAttributes:
    limits:
      cpu: 2000m
      memory: 1.3Gi
    requests:
      cpu: 500m
      memory: 1.3Gi

HealthCheck:
  Enabled: true
  EetectAttributes:
    startupProbe:
      failureThreshold: 50
      httpGet:
        path: /license/v1/actuator/spaceHealth
        port: registry
      initialDelaySeconds: 60
      periodSeconds: 30
      successThreshold: 1
      timeoutSeconds: 50
    livenessProbe:
      failureThreshold: 3
      httpGet:
        path: /license/v1/actuator/spaceHealth
        port: registry
      periodSeconds: 30
      successThreshold: 1
      timeoutSeconds: 50
    readinessProbe:
      failureThreshold: 3
      httpGet:
        path: /license/v1/actuator/spaceHealth
        port: registry
      periodSeconds: 30
      successThreshold: 1
      timeoutSeconds: 50



Tolerations:
  Enabled: false
  TolerationsAttributes:
    - key: "iam"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"




NodeSelector:
  Enabled: false
  Attributes:
    kubernetes.io/hostname: kdev-control-163.230
############################################################### 以下内容为默认配置
###定义滚动更新策略
strategy:
  enabled: true
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 2
    maxUnavailable: 0



autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

affinity: {}
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
# runAsUser: 1000