{{- if .Values.ClusterIPService.Enable }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cbb-gateway-web.fullname" . }}
  labels:
    {{- include "cbb-gateway-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ClusterIPService.type }}
  ports: {{- $type := .Values.ClusterIPService.type -}}
    {{- range .Values.ClusterIPService.ClusterExternalPorts }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .portName }}
      {{- if eq $type "NodePort" }}
      nodePort: {{ .nodePort }}
      {{- end }}
     {{- end }}
  selector:
    {{- include "cbb-gateway-web.selectorLabels" . | nindent 4 }}
{{- end }}
---
{{- if .Values.NodePortService.Enable }}
apiVersion: v1
kind: Service
metadata:
  name: headless-{{ include "cbb-gateway-web.fullname" . }}
  labels:
    {{- include "cbb-gateway-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.NodePortService.type }}
  ports: {{- $type := .Values.NodePortService.type -}}
    {{- range .Values.NodePortService.ClusterExternalPorts }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .portName }}
      {{- if eq $type "NodePort" }}
      nodePort: {{ .nodePort }}
      {{- end }}
     {{- end }}
  selector:
    {{- include "cbb-gateway-web.selectorLabels" . | nindent 4 }}
{{- end }}
---
{{- if .Values.Servicecompatible.Enabled }}
apiVersion: v1
kind: Service
metadata:
  name: auth
  labels:
    {{- include "cbb-gateway-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ClusterIPService.type }}
  ports: {{- $type := .Values.ClusterIPService.type -}}
    {{- range .Values.ClusterIPService.ClusterExternalPorts }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .portName }}
      {{- if eq $type "NodePort" }}
      nodePort: {{ .nodePort }}
      {{- end }}
     {{- end }}
  selector:
    {{- include "cbb-gateway-web.selectorLabels" . | nindent 4 }}
{{- end }}